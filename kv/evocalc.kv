<EvoCalc>
    name: 'evo_calc'
    id: evo_calc
    GridLayout:
        pos_hint: {'top': 1, 'center_x': .5}
        size_hint: (0.8, .07)
        cols: 3
    #        col_default_width:60
        rows: 1
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        Spinner:
            id: evo_spinner
            background_color: (1, 2, 1, 1)
            text: 'CHOOSE EVO'
            text_size:self.size
            halign: 'center'
            valign: 'middle'
            color: (0,0,0,1)
#            font_size:15
    #            size_hint: None, None
            size: 300, 30
            on_press:
                root.evo_spinner(self.text)
            on_text:
                root.spinner_on_text(self.text)
        Label:
            text: 'Batches/day'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
#            font_size: 15
        TextInput:
            input_filter: 'int'
            name: 'batches'
            text: ''
#            font_size:12
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)
    GridLayout:
        pos_hint: {'top': .90, 'center_x': .5}
        size_hint: (0.8, .65)
        cols: 4
        rows: 10
        spacing: 3
        font_size: 11
        Label:
            text: 'PROD.DAYS/YEAR: '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'prod_day'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'PROD.RATE: '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            id: prod_rate
            text:' %'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            text:'LATEX €/T'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'latex_price'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'EVO €/T'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'evo_price'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'LATEX DRY'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'latex_dry'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'EVO DRY'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            id: evo_dry
            text:' %'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            text:'LATEX DRY'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            id: latex_dry_euro
            text:' €/T'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            text:'EVO DRY'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            id: evo_dry_euro
            text:' €/T'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            text:'DS. LATEX'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'ds_latex'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'DS. EVO'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'ds_evo'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'LATEX/PART kg'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'float'
            name: 'latex_part'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'EVO/PART kg'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'float'
            name: 'evo_part'
            text:''
            font_size:self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'LATEX/PART kg'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            id: latex_part_dry
            text:' dry'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            text:'EVO/PART kg'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            id: evo_part_dry
            text:' dry'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

    GridLayout:
        pos_hint: {'top': .25, 'center_x': .5}
        size_hint: (0.8, .15)
        cols: 5
        rows: 2
        spacing: 5
        font_size: 11
        Label:
            text:'ST.COATING'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            text:'LATEX PARTS'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'st_latex'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'EVO PARTS'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'st_evo'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'TRIAL'
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        Label:
            text:'LATEX PARTS'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'tr_latex'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)

        Label:
            text:'EVO PARTS'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'tr_evo'
            text:''
            halign: 'left'
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.variable_input(self.text, self.name)


    GridLayout:
        id: bottom_line
        pos_hint: {'top': .08, 'center_x': .5}
        size_hint: (0.8, .08)
        rows: 1
        cols: 2
        spacing: 5
        Button:
            text:'CALCULATE'
            bold: True
            valign: 'middle'
            color: (0, 1, 0, 1)
            on_press:
                root.evo_calculations()
                root.add_cost_popup_button()
                root.button_pressed()
#            on_release:
#                root.button_pressed()


