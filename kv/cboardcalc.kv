<CBoardCalc>
    name: 'cboard_calc'
    id: cboard_calc
    GridLayout:
        pos_hint: {'top': 1, 'center_x': .5}
        size_hint: (0.8, .08)
        cols: 2
    #        col_default_width:60
        rows: 1
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        Label:
            text: 'Total starch (kg):  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

    GridLayout:
        pos_hint: {'top': 0.9, 'center_x': .5}
        size_hint: (0.8, .50)
        cols: 4
    #        col_default_width:60
        rows: 10
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        Label:
            text: 'E-flute [qm]:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'E-flute coef.:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'B-flute [qm]:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'B-flute coef.:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'C-flute [qm]:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'C-flute coef.:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'BC-flute [qm].:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'BC-flute coef.:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'EB-flute [qm].:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'EB-flute coef.:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'EC-flute [qm].:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

        Label:
            text: 'EC-flute coef.:  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            text:''
            font_size:15
            multiline: False
#            on_text_validate:
#                root.vector_production(self.text)

    GridLayout:
        pos_hint: {'top': 0.4, 'center_x': .5}
        size_hint: (0.8, .08)
        cols: 2
    #        col_default_width:60
        rows: 1
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        Button:
            text: 'Calculate[g/m2]: '
        Label:
            text:''