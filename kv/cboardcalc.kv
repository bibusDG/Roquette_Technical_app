<CBoardCalc>
    name: 'cboard_calc'
    id: cboard_calc
    GridLayout:
        pos_hint: {'top': 1, 'center_x': .5}
        size_hint: (0.8, .07)
        cols: 1
    #        col_default_width:60
#        rows: 1
#        row_default_height: 30
#        row_force_default: True
        spacing: 1
        Spinner:
            id: cboard_spinner
            background_color: (1, 2, 1, 1)
            text: 'STARCH RECIPE'
            color: (0,0,0,1)
            font_size:20
    #            size_hint: None, None
            size: 300, 30
            on_press:
                root.cboard_spinner(self.text)
            on_text:
                root.cboard_spinner_starch(self.text)
    GridLayout:
        pos_hint: {'top': .9, 'center_x': .5}
        size_hint: (0.8, .08)
        cols: 2
    #        col_default_width:60
        rows: 1
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        Label:
            text: 'Total starch (kg):  '
            text_size: self.size
            bold: True
            halign: 'left'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: 15

        TextInput:
            input_filter: 'int'
            name: 'tot_starch'
            text:''
            font_size:15
            multiline: False
            on_text_validate:
                root.total_starch(self.text, self.name)

    GridLayout:
        pos_hint: {'top': 0.80, 'center_x': .5}
        size_hint: (0.8, .55)
        cols: 4
    #        col_default_width:60
        rows: 10
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        font_size: 14
        Label:
            text: 'E [qm]:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name:'e_total'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.amount(self.text, self.name)

        Label:
            text: 'E coef.:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            ffont_size: self.parent.font_size

        TextInput:
            input_filter: 'float'
            name: 'e_coef'
            text:'1'
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text, self.name)

        Label:
            text: 'B [qm]:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'b_total'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.amount(self.text, self.name)

        Label:
            text: 'B coef.:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'float'
            name: 'b_coef'
            text:'1'
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text, self.name)

        Label:
            text: 'C [qm]:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filer: 'int'
            name: 'c_total'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.amount(self.text, self.name)

        Label:
            text: 'C coef.:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter : 'float'
            name: 'c_coef'
            text:'1'
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text, self.name)

        Label:
            text: 'BC [qm].:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'bc_total'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.amount(self.text, self.name)

        Label:
            text: 'BC coef.:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'float'
            name: 'bc_coef'
            text:'2'
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text, self.name)

        Label:
            text: 'EB [qm].:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'be_total'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.amount(self.text, self.name)

        Label:
            text: 'EB coef.:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'float'
            name: 'be_coef'
            text:'2'
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text, self.name)

        Label:
            text: 'EC [qm].:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'ce_total'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.amount(self.text, self.name)

        Label:
            text: 'EC coef.:  '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'float'
            name: 'ce_coef'
            text:'2'
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text, self.name)
    GridLayout:
        pos_hint: {'top': 0.27, 'center_x': .5}
        size_hint: (0.8, .16)
        cols: 4
    #        col_default_width:60
        rows: 2
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        font_size: 14
        Label:
            text:'NaOH kg: '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size
        TextInput:
            input_filter: 'float'
            name: 'sode_kg'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text,self.name)

        Label:
            text:'NaOH conc.: '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size
        TextInput:
            input_filter: 'int'
            name: 'sode_conc'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text,self.name)

        Label:
            text:'New conc.: '
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            font_size: self.parent.font_size

        TextInput:
            input_filter: 'int'
            name: 'new_sode_conc'
            text:''
            font_size: self.parent.font_size
            multiline: False
            on_text_validate:
                root.coeficient(self.text,self.name)

        Button:
            text: 'New dosage: '
            font_size: self.parent.font_size
            on_press:
                root.sode_calculation()
        Label:
            id: new_sode_kg
            text: 'kg'
            text_size: self.size
            bold: True
            halign: 'right'
            valign: 'middle'
            color: (0, 0, 0, 1)
            ffont_size: self.parent.font_size

#        Button:
#            text: 'Calculate[g/m2]: '
#            on_release:
#                root.calculate()
#        Label:
#            id: result
#            text:' g/m2'

    GridLayout:
        pos_hint: {'top': 0.1, 'center_x': .5}
        size_hint: (0.8, .08)
        cols: 2
    #        col_default_width:60
        rows: 1
#        row_default_height: 30
#        row_force_default: True
        spacing: 3
        Button:
            text: 'Calculate[g/m2]: '
            on_release:
                root.calculate()
        Label:
            id: result
            text:' g/m2'